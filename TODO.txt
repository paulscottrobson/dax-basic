TODO:
	GOSUB .. RETURN, open and close frame, read and write stack address.
	REPEAT..UNTIL
	IF...THEN/GOTO... ELSE
	FOR..TO..STEP..NEXT
	VDU (with semicolons)
	READ/DATA/RESTORE
	DEF PROC ... ENDPROC / LOCAL
	LIST

Notes:	
	call RESTORE in RUN when READ/DATA/RESTORE working.
