TODO:
	Fix STR$ and PRINT to output hex ?
		- STR$~(x) does it in hex
		- PRINT ~ switches to hex, comma or semicolon switches back to decimal.
	Array code in find.asm (requires DIM) and add to variables.
	$<test> requires DIM (TOP ?)
	GOTO .. 
	Implement Return Stack.
	GOSUB .. RETURN
	REPEAT..UNTIL
	IF...THEN/GOTO... ELSE
	FOR..TO..STEP..NEXT
	VDU (with semicolons)
	READ/DATA/RESTORE
	DEF PROC ... ENDPROC / LOCAL
	LIST

Notes:	
	call RESTORE in RUN when READ/DATA/RESTORE working.

