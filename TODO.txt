TODO:
	GOTO .. 
	GOSUB .. RETURN
	REPEAT..UNTIL
	IF...THEN/GOTO... ELSE
	FOR..TO..STEP..NEXT
	VDU (with semicolons)
	READ/DATA/RESTORE
	DEF PROC ... ENDPROC / LOCAL
	LIST

Notes:	
	call RESTORE in RUN when READ/DATA/RESTORE working.
